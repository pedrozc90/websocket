services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        image: websocket:dev
        # container_name: websocket
        ports:
            - "4000:4000"
        environment:
            NODE_ENV: production
            PORT: 4000
            LOGGER_LEVEL: "info"
